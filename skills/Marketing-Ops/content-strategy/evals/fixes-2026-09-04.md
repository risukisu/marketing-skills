# Dry-run findings and fixes — 2026-09-04

Two end-to-end dry runs (Sonnet subagents playing both roles) against the first complete
version of the skill. Transcripts: `dryrun-build-2026-09-04.md` (Ledgerline, fictional UK
payroll consultancy) and `dryrun-audit-2026-09-04.md` (Northgate Freight, fictional
Polish/Baltic logistics firm).

## Results

- Build: all nine output sections present; twelve-slot hub assembled; unknown numbers labeled
  unknown; default filename used; copy-deslop step noted.
- Audit: passive gate → offer → "audit" → intake → seven parts scored → 1% test = 2 of 4
  (mission, research true; influencer, guest posting false) → ranked gaps → next-quarter hub.

## Gaps the runs exposed, and what changed

| Finding (from the run) | Fix |
|---|---|
| Routing table had no case for "lots of content, none of the seven parts" — could silently default to Audit | SKILL.md Step 0: explicit third routing rule → recommend Build, say why, user may still pick Audit |
| Hub template assumed a research anchor; nothing said what to do when step 3 is empty | `content-hub-template.md`: new section "When there is no research anchor yet" (foundation hub, research designed in Q1, anchors Q2) |
| Data-hook instruction lived only in the Build table; audit had to jump back; risk of re-asking known unknowns and of not saying *why* a number is unknown | SKILL.md: new "Data hooks (both flows)" section; record unknown once, reuse, state the reason in the doc |
| 5–10 named people/sites required but user could only give roles | SKILL.md data-hooks section: accept roles as placeholders, naming becomes a week-one hub task |
| Audit flow only told the agent to read the rubric, not the part references | SKILL.md Audit step 1: read the part's reference when scoring or writing its fix |
| Mission "Present" needed three recent titles; no rule when titles weren't checked | `audit-rubric.md`: Present + "fit unverified" if published; Partial if unpublished |
| Gap ranking handled "converts at zero" but not "conversion unknown" | `audit-rubric.md`: jumps to first if unknown + Missing; stays with "measure it" as first fix line if unknown + Partial |
| "Quote the evidence" assumed URLs/numbers | SKILL.md + rubric: URL, number, or the user's sentence in quotes, labeled as which |

Not a skill defect: the build eval's fixed fact-set meant the mission was drafted by Claude
and rubber-stamped rather than elicited. A live user supplies that language.

## Re-test

See the bottom of this file for the focused re-test of routing and the no-research hub.

### Re-test result (fresh Sonnet subagent, after fixes)

- Routing check (40 posts, no mission/research/guest posts): routed to **Build**, quoting the new
  Step 0 rule verbatim; reported the rule as explicit, not inferred.
- No-research hub check: produced twelve slots following the new "When there is no research
  anchor yet" section, with substituted slots labeled "anchor: research from Q2"; reported the
  rule as explicit, not invented.

## 2026-09-05: context handling redesigned (owner review)

Owner: the skill must not assume a folder structure for company context, and must never use found
context without confirmation. Replaced "Context detection" (`*-context.local.md` packs as primary)
with a "Company context: setup step, every run": scan the launch directory, list filenames only,
ask (use / redirect / paste / none), read only what's approved, earlier outputs double as memory,
local packs are an optional extra under the same rule.

Re-test (fresh Sonnet subagent, simulated scan): turn 1 listed four filenames and asked; turn 2
read only the three approved files and left the excluded audit unread, then asked the intake;
turn 3 routed to Audit. Subagent confirmed SKILL.md never permits reading before approval, quoting
"List filenames only. Don't read contents yet." and "Read only what the user approved."
